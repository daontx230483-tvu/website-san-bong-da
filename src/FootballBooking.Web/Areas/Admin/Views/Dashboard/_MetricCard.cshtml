@model FootballBooking.Application.Reports.SummaryMetricDto

<section class="fb-card p-4">
    <p class="text-sm text-muted">@Model.Label</p>
    <p class="mt-2 text-2xl font-semibold text-ink">@Model.Value</p>
    <div class="mt-3">
        <partial name="~/Views/Shared/Partials/_StatusBadge.cshtml" model='new StatusBadgeViewModel(Model.Note, Model.Tone)' />
    </div>
</section>
